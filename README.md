# Динамический массив на языке C (Лабораторная работа)

Проект представляет собой реализацию динамического массива с поддержкой полиморфизма. Система позволяет работать с массивами разных типов данных (`int`, `double`), используя унифицированный интерфейс, и включает в себя инструменты для автоматического тестирования и управления памятью.

## Автор
**Студент:** Камышев А.С.  
**Группа:** Б25-517

## Особенности реализации
* **Модульная архитектура:** Проект разделен на логические слои (описание типов, логика массива, тесты, интерфейс), что обеспечивает чистоту кода и легкую масштабируемость.
* **Полиморфизм:** Реализован с помощью структуры-«паспорта» (`TypeInf`), что позволяет функциям (сортировка, маппинг, фильтрация) работать с любыми типами данных через `void*`.
* **Управление памятью:** Использование `malloc`, `realloc` и `free` с защитой от ошибок и утечек.
* **Автоматизированное тестирование:** Встроена система юнит-тестов с использованием `assert`, позволяющая быстро проверять корректность работы алгоритмов.

## Структура проекта
* `main.c` — точка входа и диспетчер меню.
* `dynamic_array.c / .h` — ядро проекта, реализация структуры динамического массива.
* `data_types.c / .h` — описание типов данных (`int`, `double`) и методов работы с ними.
* `tests.c` — модуль автоматического тестирования логики.
* `manual.c` — модуль интерактивного взаимодействия с пользователем.
* `Makefile` — скрипт для автоматизации сборки проекта.

## Сборка и запуск

Для компиляции проекта используется `gcc`. В проекте предусмотрен `Makefile`.

1. **Сборка проекта:**
   Откройте терминал в папке с проектом и выполните:
   ```bash
   make
2. **Запуск программы**
   ```bash
   ./my_app
3. **Очистка файлов сборки**
      ```bash
    make clean


## Режимы работы
Программа поддерживает два основных режима:

Автоматический тест: Запускает набор проверок (сортировка, маппинг, фильтрация) для подтверждения работоспособности всех функций массива.

Ручной режим: Позволяет пользователю выбрать тип данных, наполнить массив элементами, выполнить сортировку и другие операции в интерактивном режиме.
